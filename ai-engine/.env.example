# App
ENVIRONMENT=production

# Model settings
MODEL_NAME=gpt-4o-mini
TEMPERATURE=0.7
MAX_TOKENS=1000

# Backend — use internal URL if services are in the same VPC/cluster
BACKEND_URL=http://<backend-internal-url>:8448

# OpenAI Credentials
OPENAI_API_KEY=
MODEL_NAME=
OPENAI_BASE_URL=